# bps: beacon-receiver

```bash
@bps beacon - \
    --x 1.1 \
    --y 2.2 \
    --sigma 3.3 \
    --timeout 10
```

```text

```

on another pi,

```bash
@bps receiver - \
    --grep sparrow \
    --timeout 10
```

```text

```

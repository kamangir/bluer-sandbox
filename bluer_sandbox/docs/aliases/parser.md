# aliases: parser

an html parser.

```bash
@parser \
	parse \
	[dryrun,~upload] \
	<url> \
	[-|<object-name>] \
	[--depth <depth>] \
	[--filename <filename>] \
	[--root 0]
 . parse <url> -> <object-name>.
```

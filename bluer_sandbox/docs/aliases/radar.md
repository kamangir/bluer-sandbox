# aliases: radar

a web radar.

```bash
@radar \
	fetch \
	[~download,upload] \
	<url> \
	[-|<object-name>] \
	[--filename <filename>] \
	[--max_iteration <5>] \
	[--root 0] \
	[--seed <url>]
 . fetch <url> -> <object-name>.
```

# aliases: radar

a web radar.

```bash
@radar \
	fetch \
	[~download,~upload] \
	<url> \
	[-|<object-name>] \
	[--depth <depth>] \
	[--filename <filename>] \
	[--root 0] \
	[--seed <url>]
 . fetch <url> -> <object-name>.
```

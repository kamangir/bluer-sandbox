# aliases: radar

a web radar.

```bash
@radar \
	fetch \
	[dryrun,~upload] \
	<url> \
	[-|<object-name>] \
	[--depth <depth>] \
	[--filename <filename>] \
	[--root 0]
 . fetch <url> -> <object-name>.
```
